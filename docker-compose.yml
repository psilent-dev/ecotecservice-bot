version: "3.9"
services:
  bot:
    build: .
    container_name: ecotec_bot
    restart: unless-stopped
    env_file: .env  # BOT_TOKEN, ADMIN_CHAT_ID — те же имена, что на сайте Mini App
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    environment:
      - TZ=Europe/Moscow
